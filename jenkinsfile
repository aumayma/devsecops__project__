pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'main', url: 'https://github.com/aumayma/devsecops__project__.git'
            }
        }

stage('Run Gitleaks') {
            steps {
                script {
                    def result = sh(script: 'gitleaks detect --source . --verbose --redact', returnStatus: true)
                    if (result != 0) {
                        error("❌ Des secrets ont été détectés ! Corrige-les avant de pousser le code.")
                    }
                }
            }
        }
              
}
}
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Compilation en cours..."
            }
        }
    }
    post {
        failure {
            emailext subject: "🚨 Build Failed : #${BUILD_NUMBER}",
                     body: """
                     Bonjour Team,

                     Le build #${BUILD_NUMBER} a échoué sur Jenkins.

                     💡 Vérifiez les logs : ${BUILD_URL}

                     -- 
                     Cordialement,
                     Jenkins
                     """,
                     to: 'selmioumayma89@gmail.com'
        }
    }
}

